name: Reproduce NuGet Audit Issue

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-without-matcher:
    name: "Build WITHOUT msbuild matcher (current behavior)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET 
        # uses: actions/setup-dotnet@v6
        uses: Falco20019/setup-dotnet@feature/msbuild-matcher
        with:
          dotnet-version: "8.0.x"

      - name: Build project
        run: dotnet build sample-app/sample-app.csproj



 